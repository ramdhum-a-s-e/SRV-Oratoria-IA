# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
pydantic==2.13.3
pydantic_core==2.46.3

# ── Base de datos ──────────────────────────────────────────────────────────────
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.1

# ── Auth ───────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4

# ── Dimensión 1: Fluidez Mecánica y Prosodia ──────────────────────────────────
faster-whisper==1.2.1       # ASR: transcripción + timestamps por palabra
praat-parselmouth==0.4.5    # Análisis prosódico: F0, jitter, shimmer, HNR
ctranslate2==4.7.1          # Backend de faster-whisper (GPU/CPU)

# ── Dimensión 2: Claridad y Coherencia (Sprint 2) ─────────────────────────────
# spacy==3.8.7              # NLP español (instalar con: python -m spacy download es_core_news_lg)
# sentence-transformers     # Coherencia semántica con BETO

# ── Dimensión 3: Seguridad Emocional (Sprint 2) ───────────────────────────────
# transformers              # Wav2Vec2-SER
# opensmile                 # IS13-ComParE features

# ── Audio utils ────────────────────────────────────────────────────────────────
av==17.0.1                  # Decodificación de audio (PyAV)
numpy==2.4.4

# ── Utilidades ─────────────────────────────────────────────────────────────────
python-dotenv==1.1.0
huggingface_hub==1.13.0

# ── Validación D1 (solo desarrollo) ───────────────────────────────────────────
gtts==2.5.4                 # Generar audio de prueba en español
sounddevice==0.5.5          # Grabación desde micrófono